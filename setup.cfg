[develop]
script_dir=$base/lib/hsm_robot
[install]
install_scripts=$base/lib/hsm_robot

[tool:pytest]
markers =
    linter: the L0 tier - the coding style of the package
    node: the L2 tier - one framework node driven through the ROS2 objects it declares
timeout = 60
